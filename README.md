# AlphaQuant India Dashboard
1. Clone the repo.
2. Install dependencies: `pip install -r requirements.txt`.
3. Run the app: `streamlit run app.py`.
4. Upload a CSV with `Ticker, Avg_Price, Qty` to analyze your portfolio.
   
